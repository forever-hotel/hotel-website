# Keep the build context small and never send secrets to the Docker daemon.
node_modules
dist
coverage
test
*.log

# Environment files contain credentials; configuration is passed at runtime.
.env
.env.*

# Local tooling and docs
.git
.vscode
.idea
*.md
Dockerfile
.dockerignore
